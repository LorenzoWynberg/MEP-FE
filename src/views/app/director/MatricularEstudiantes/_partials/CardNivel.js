import React from 'react'
import { makeStyles } from '@material-ui/core/styles'

const CardNivel = (props) => {
  const classes = useStyles()

  return (
    <div />

  )
}

const useStyles = makeStyles((theme) => ({
  cardContainer: {

  }

}))
export default CardNivel
